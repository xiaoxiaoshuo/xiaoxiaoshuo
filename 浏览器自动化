# 浏览器自动化技术笔记

## CDP 协议

目前最新的技术就是 CDP 的各种封装。**CDP**(Chrome DevTools Protocol,Chrome 开发者工具协议)是浏览器的开发调试环境,相当于浏览器的正宗后门。

---

## 发展历程

- **2017 年**:无头 Chrome (Headless Chrome) 和 **Puppeteer** 发布(基于 Node.js 的浏览器自动化库,利用 CDP 进行通信)
- **2020 年 1 月 31 日**:**Playwright** 由 Microsoft 发布并开源,将 CDP 发扬光大

Puppeteer 的实践用法:做网页版 PDF 批量导出,非常顺手。

---

## 轻量化方案

现在的 **OpenClaw** 使用 `playwright-core` 的轻量化库。

---

## 连接已有浏览器会话的两种方式

| 特性 | Chrome 扩展中继 (`extension-relay.js`) | 远程调试端口 (`--remote-debugging-port`) |
|------|------|------|
| **配置复杂度** | 高(需安装扩展) | 低(启动参数) |
| **稳定性** | 一般(可能断开) | 高(标准协议) |
| **真实用户环境** | 支持 | 不支持(新实例) |
| **已登录状态** | 支持 | 不支持 |
| **反爬虫绕过** | 较好 | 较差(无头特征) |
| **安全性** | 存在潜在隐患 | 标准且相对安全 |

本质上是**是否复用已有用户会话**的选择。

---

## 缺点与对比

- 相比纯视觉方案,反爬虫能力不够
- 遇到弹窗提示时,很难捕获到输入框或按钮的错误提示
- **Manus** 在这方面做得很好,相当于录播回放 + 人在回路的控制接管

---

## 更彻底的方案

[Ego App](https://lite.ego.app/zh-cn) 做得更彻底--原生封装了浏览器,更快、更彻底。开发者牛逼,对ai有深入理解,对ai调用和cli有清晰理解,用语言层面的优化,代理多轮次toolcall,有想法
snapshot 问题有解决, Accessibility.getFullAXTree 获取无障碍树,浏览器原生提供

 openclaw 总结流程图:

 ```
   Browser Tool: snapshot()
     │
     ├─ format="aria"?
     │   └─ CDP Accessibility.getFullAXTree → 原始 AX 节点列表
     │
     └─ format="ai" (默认)?
         │
         ├─ 简单模式(无额外参数)
         │   └─ page._snapshotForAI() → 纯文本树(无 ref)
         │
         └─ 带 labels/interactive/compact/depth/refs
             │
             ├─ refs="aria" → _snapshotForAI() + 保留原生 ref ID
             └─ refs="role" → page.ariaSnapshot() + 分配新 ref ID
 ```
总结:只靠 Accessibility.getFullAXTree  是不行的,只用 AX 树会漏掉 60%+ 的模拟按钮,必须配合 JS 查询。但 AX 树的优势是能提供语义化的 name 和 role,所以两者结合是最佳方案

──────────────────────┬─────────┬─────────────┬──────────────────┐
 │ 元素                 │ AX 树   │ JS 补充     │ 说明             │
 ├──────────────────────┼─────────┼─────────────┼──────────────────┤
 │ <button> 真按钮      │ ✅ 识别 │ -           │ 标准元素         │
 ├──────────────────────┼─────────┼─────────────┼──────────────────┤
 │ <div onclick> 裸 div │ ❌ 漏掉 │ ✅ 抓到     │ AX 不认          │
 ├──────────────────────┼─────────┼─────────────┼──────────────────┤
 │ <div role="button">  │ ✅ 识别 │ ✅ 重复抓到 │ 两边都有,需去重 │
 ├──────────────────────┼─────────┼─────────────┼──────────────────┤
 │ <span onclick>       │ ❌ 漏掉 │ ✅ 抓到     │ AX 不认          │
 ├──────────────────────┼─────────┼─────────────┼──────────────────┤
 │ <div class="btn-*">  │ ❌ 漏掉 │ ✅ 抓到     │ AX 不认          │
 ├──────────────────────┼─────────┼─────────────┼──────────────────┤
 │ <div onclick> 裸 div │ ❌ 漏掉 │ ✅ 抓到     │ AX 不认          │
 └──────────────────────┴─────────┴─────────────┴─────────────────

## 大型网站,国内的知乎,和小红书,boss等平台反扒严格
- 灰产还带多了解,比如怎么改boss的防浏览器调试

---

## 2026-08-07 更新：pi-chrome-use 研究结论

> 详细研究结论已单独整理到新文件：→ **[pi-chrome-use-研究结论.md](./pi-chrome-use-研究结论.md)**

核心要点：
- **pi-chrome-use** 是给 Pi 用的轻量 CDP 扩展，控制你已有的标准 Chrome
- **AX 树 + JS 查询混合**是获取可交互元素的最佳方案（只用 AX 树会漏 60%+ 的 div 模拟按钮）
- **ego-lite** 是定制 Chromium，有内核级 snapshot，但需要安装定制浏览器

