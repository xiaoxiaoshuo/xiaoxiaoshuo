# 浏览器自动化 & Chrome 知识总结

> 日期：2026-08-07
> 主题：pi-chrome-use 扩展、Chrome 启动参数、CDP 协议

---

## 一、pi-chrome-use 扩展

| 知识点 | 内容 |
|-------|------|
| **是什么** | 一个 Pi 扩展，让 Pi 能通过 CDP 协议驱动真实浏览器 |
| **安装方式** | `pi install npm:pi-chrome-use` |
| **核心工具** | `browser_execute` — 在浏览器中执行 JavaScript |
| **API 流程** | `session.connect()` → `session.use(targetId)` → `session.Page.navigate()` / `session.Runtime.evaluate()` / `session.Page.captureScreenshot()` |
| **关键点** | 连的是**你已有的浏览器**，不是启动新实例 |

### browser_execute 代码模板

```javascript
// 1. 连接浏览器
await session.connect({ wsUrl: 'ws://localhost:9222/devtools/browser/<id>' });

// 2. 复用已有 tab（不要每次新建！）
const { targetInfos } = await session.Target.getTargets({});
const pages = targetInfos.filter(t => t.type === "page" && !t.url.startsWith("chrome://"));
let target = pages[0];
if (!target) {
  const r = await session.Target.createTarget({ url: 'about:blank' });
  target = { targetId: r.targetId };
}
await session.use(target.targetId);

// 3. 干活
await session.Page.navigate({ url: 'https://...' });
const { result } = await session.Runtime.evaluate({ expression: 'document.title' });

// 4. 截图（自动转成 Pi 图片结果）
const { data } = await session.Page.captureScreenshot({ format: 'png' });

// 5. 如果是新建的 tab，干完活关掉
await session.Target.closeTarget({ targetId: target.targetId });
```

---

## 二、pi-chrome-use vs Playwright

### 为什么不用 Playwright？

| 对比项 | pi-chrome-use（裸 CDP） | Playwright |
|--------|----------------------|------------|
| **连接方式** | 原生 WebSocket 直连 CDP | 高层 API 封装 |
| **包体积** | ~200KB | ~5MB (core) / ~30MB (完整) |
| **依赖** | 零额外依赖（Node.js 内置 WebSocket） | 需下载 Chromium 等 |
| **定位** | 连已有浏览器干活 | 启动新浏览器测试 |
| **控制粒度** | 可拦截每个 CDP 消息 | 封装在 Page/BrowserContext 后 |

### Playwright 到底有什么用？

- **CI/CD 自动化测试**：跨浏览器（Chromium + Firefox + WebKit）跑测试用例
- **无头浏览器大规模爬虫**：启动独立实例
- **网络拦截 / Mock API**：`route()` 拦截请求
- **移动设备模拟**：iPhone / Pixel 预设
- **截图 / PDF 生成**

### 一句话对比

> **Playwright = "帮你启动一个浏览器来测试"**
> **pi-chrome-use = "连到你已有的浏览器来干活"**

---

## 三、Chrome 启动参数

### 远程调试相关

| 参数 | 说明 |
|------|------|
| `--remote-debugging-port=9222` | **开启调试端口**，pi-chrome-use 靠这个连 |
| `--remote-debugging-address=0.0.0.0` | 允许局域网连接（默认只允许本地） |
| `--remote-allow-origins=*` | Chrome 128+ 需要，允许跨域连接调试 |

### 测试 / 自动化相关

| 参数 | 说明 |
|------|------|
| `--headless` | 无头模式，不显示窗口（老版） |
| `--headless=new` | 新版无头模式，Chrome 112+ |
| `--no-first-run` | 跳过首次运行向导 |
| `--no-default-browser-check` | 不检查默认浏览器 |
| `--disable-gpu` | 禁用 GPU 加速 |
| `--window-size=1920,1080` | 设置窗口大小 |
| `--start-maximized` | 最大化启动 |

### 用户数据 / 配置

| 参数 | 说明 |
|------|------|
| `--user-data-dir=/path` | 指定用户数据根目录（含登录态、Cookie、扩展） |
| `--profile-directory=Name` | 指定使用哪个用户配置（Default、Profile 1 等） |
| `--incognito` | 无痕模式 |

### 实际组合

```bash
# 1. 日常开发：保留登录态 + 调试端口
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome \
  --remote-debugging-port=9222 --no-first-run --no-default-browser-check

# 2. 独立配置文件（不污染日常浏览器）
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome \
  --remote-debugging-port=9222 --user-data-dir=/tmp/chrome-test-profile --no-first-run

# 3. 无头模式自动化
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome \
  --headless=new --remote-debugging-port=9222 --no-first-run --no-sandbox --disable-gpu
```

---

## 四、--user-data-dir 与 --profile-directory 的关系

```
--user-data-dir = 整栋公寓楼
--profile-directory = 楼里的独立房间
```

### 实际目录结构

```
~/Library/Application Support/Google/Chrome/   ← --user-data-dir
  │
  ├── Local State              ← ✅ 全局共享（浏览器级设置、profile 列表）
  ├── DevToolsActivePort       ← ✅ 全局共享（调试端口文件，pi-chrome-use 找的就是这个）
  ├── First Run / Crash Reports / Safe Browsing  ← ✅ 全局共享
  │
  ├── Default/                 ← 用户1（--profile-directory=Default）隔离
  │     ├── Preferences
  │     ├── Cookies
  │     ├── Login Data
  │     ├── History
  │     ├── Bookmarks
  │     ├── Extensions/
  │     └── Local Storage/
  ├── Profile 1/               ← 用户2（隔离）
  └── Profile 2/               ← 用户3（隔离）
```

### 共享 vs 隔离

**共享的（user-data-dir 根目录）：**
- Local State（全局配置、上次使用的用户）
- DevToolsActivePort（调试端口）
- First Run / Crash Reports / Safe Browsing

**隔离的（每个 Profile 独立）：**
- Cookies、浏览历史、书签、密码、扩展、LocalStorage、IndexedDB、网站权限、主题、打开的标签页

### 关键区别

| | `--user-data-dir` | `--profile-directory` |
|---|---|---|
| **本质** | 目录（文件夹） | 子目录名（文件夹名） |
| **类比** | 整栋楼 | 楼里的一个房间 |
| **默认值** | 系统固定路径 | `Default` |
| **隔离级别** | 完全隔离 | 同目录下切换用户 |

**一句话：**
- 换 `--user-data-dir` = 换一套完全独立的 Chrome（像换了台电脑）
- 换 `--profile-directory` = 切换 Chrome 里的不同用户（像切换账号）

**pi-chrome-use 的 `session.connect({ profileDir })` 传的就是 `--user-data-dir` 的路径，让它去读目录下的 `DevToolsActivePort` 文件。**

---

## 五、写浏览器代码的好习惯

| 之前的做法 ❌ | 正确的做法 ✅ |
|-------------|-------------|
| 每次都 `createTarget` 开新 tab | 先 `Target.getTargets` 找已有 tab 复用 |
| 干完活不关 tab | 新建的 tab 用 `Target.closeTarget` 关掉 |
| 每次重新连接 | 同一 session 复用 |

**原则：复用已有 tab（省内存、保留历史/登录态/DOM 状态），只关自己新建的临时 tab。**

---

## 六、一次性速查命令

```bash
# 查看 Chrome 是否运行 & 调试端口
ps aux | grep -i chrome | grep -v grep
cat ~/Library/Application\ Support/Google/Chrome/DevToolsActivePort 2>/dev/null
lsof -i :9222

# 验证调试端口
curl -s http://localhost:9222/json/version | python3 -m json.tool

# 查看 Chrome 当前启动参数
# 地址栏输入 chrome://version ，看"命令行"一栏
```

---

*本文档由学习对话总结生成，用于记录浏览器自动化相关知识点。*